# Спільні уроки tg-router-bot (авто-журнал провалів і рецептів)
25.08 | FAIL | ІНЦИДЕНТ: hotplug-скрипт з `nft flush table inet fw4` при буті витер УСІ правила fw4 -> зник masquerade -> LAN без інтернету до ручного втручання. ПРАВИЛО: ніколи не флашити цілу nft/iptables таблицю; скидання conntrack-кешу = тільки `conntrack -F` (пакет conntrack). Команди nft flush / iptables -F тепер жорстко забанені в cmd_banned.
25.08 | OK | Zaborona.help для vk.com (якщо колись знадобиться): dnsmasq upstreams ТІЛЬКИ 208.67.222.222/.220 + noresolv=1 (OpenDNS віддає внутрішню 10.224.0.30, що маршрутизиться лише через tun; змішування з 8.8.8.8 ламає схему); mssfix 1300 обовʼязковий; клієнти мусять бути на WiFi.
